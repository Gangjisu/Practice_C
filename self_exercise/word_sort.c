#include <stdio.h>

int main()
{
    /*
        1.길이가 짧은 것부터
        2.길이가 같으면 abcd순으로
        3.입력은 알파벳 소문자만 받아둘것
        4.최초실행시 몇개의 단어(변수)를 받을 지 입력받기
        5.받을때마다 50개 한정으로 받아야한다.
        6.중복 수열으로 구현

        13x50  2차원 배열 이용

        char *voca[i]; i = 13;로 되어지는 문자열 포인터 이용
        heap에다가 할당 받기
    */

    return 0;
}